<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.app.codered.lifeline_again.MainActivity">
    <EditText
        android:id="@+id/name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"

        android:layout_marginTop="34dp"
        android:ems="10"
        android:hint="enter_name"
        android:inputType="textPersonName" />

    <Spinner
        android:id="@+id/bloodgrp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/pushbutton"
        android:layout_below="@+id/name"
        android:layout_marginTop="52dp"
        android:entries="@array/bloodgrp" />

    <Button
        android:id="@+id/pushbutton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/button3"
        android:layout_marginBottom="32dp"
        android:layout_marginEnd="21dp"
        android:layout_toStartOf="@+id/tick"
        android:onClick="add"
        android:text="add" />

    <ImageView
        android:id="@+id/tick"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/pushbutton"
        android:layout_alignEnd="@+id/name"
        android:contentDescription=""
        android:visibility="invisible"
        app:srcCompat="@drawable/newgreentick"
        tools:ignore="ContentDescription" />

    <Button
        android:id="@+id/button3"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignStart="@+id/pushbutton"
        android:layout_marginBottom="25dp"
        android:onClick="change"
        android:text="link" />


</LinearLayout>
